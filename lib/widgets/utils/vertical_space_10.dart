import 'package:flutter/material.dart';

class VerticalSpace10 extends StatelessWidget {
  const VerticalSpace10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
