import 'package:flutter/material.dart';

class VerticalSpace20 extends StatelessWidget {
  const VerticalSpace20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
